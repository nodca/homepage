---
import CloudMonitor from "../components/CloudMonitor.astro";

const targets = [
	{ href: "https://blog.cyb1.org", label: "Blog" },
	{ href: "https://s.cyb1.org", label: "Tools" },
	{ href: "https://huggingface.co/wcn123/Qwen3.5-27B-WebNovel-Writer-zh", label: "Writer Model" },
	{ href: "https://github.com/nodca/routellm", label: "RouteLLM" },
	{ href: "http://38.76.207.72:3000", label: "Git Search" },
	{ href: "https://github.com/nodca", label: "GitHub" },
];
---

<html lang="zh-CN">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta
			name="description"
			content="nodca 的个人网站，记录开源项目、AI 模型、技术写作与正在构建的新东西。"
		/>
		<title>CYB1.ORG // NODCA</title>
	</head>
	<body>
		<video autoplay muted loop playsinline poster="/bg-poster.jpg" class="video-bg" data-nosnippet>
			<source src="/bg.mp4" type="video/mp4" />
		</video>
		<div class="subtitle-fade" aria-hidden="true"></div>

		<main class="vn-stage">
			<header class="status-shell" aria-label="Top status bar">
				<CloudMonitor />
			</header>

			<section class="subtitle-shell" aria-label="Profile subtitle">
				<p class="subtitle-kicker">CYB1.ORG</p>
				<h1 class="subtitle-title">nodca</h1>
				<p class="subtitle-line">Developer, writer, and builder of useful internet things.</p>
				<p class="subtitle-line subtitle-line-zh">
					中文网文写作模型已经做完一版，后续随缘优化，眼下去做新的东西。
				</p>
				<p class="subtitle-note">A personal log of tools, models, writing, and whatever comes next.</p>
			</section>

			<nav class="edge-nav" aria-label="External links">
				{
					targets.map((target) => (
						<a href={target.href} target="_blank" rel="noreferrer">
							<span class="nav-arrow" aria-hidden="true">→</span>
							<span class="nav-label">{target.label}</span>
						</a>
					))
				}
			</nav>
		</main>
	</body>
</html>

<style is:global>
	@import url("https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@500;600&family=JetBrains+Mono:wght@400;500&family=Noto+Serif+SC:wght@400;500;600&display=swap");

	:root {
		--text-primary: rgba(255, 250, 244, 0.97);
		--text-secondary: rgba(255, 250, 244, 0.8);
		--text-muted: rgba(255, 250, 244, 0.68);
		--serif-display: "Cormorant Garamond", "Noto Serif SC", "Source Han Serif SC", serif;
		--serif-body: "Noto Serif SC", "Source Han Serif SC", "Songti SC", serif;
		--mono: "JetBrains Mono", "SFMono-Regular", "Cascadia Mono", monospace;
		--subtitle-shadow: 0 2px 15px rgba(0, 0, 0, 0.42), 0 0 40px rgba(0, 0, 0, 0.2);
		--mono-shadow: 0 1px 10px rgba(0, 0, 0, 0.42), 0 0 24px rgba(0, 0, 0, 0.16);
	}

	* {
		box-sizing: border-box;
		margin: 0;
		padding: 0;
	}

	html {
		background: #050505;
	}

	body {
		min-height: 100vh;
		color: var(--text-primary);
		position: relative;
		overflow: hidden;
		background: #050505;
		isolation: isolate;
	}

	a {
		color: inherit;
	}

	.video-bg {
		position: fixed;
		inset: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center;
		pointer-events: none;
		z-index: 0;
	}

	.subtitle-fade {
		position: fixed;
		inset: auto 0 0 0;
		height: min(42vh, 24rem);
		background: linear-gradient(to top, rgba(0, 0, 0, 0.4) 0%, rgba(0, 0, 0, 0.17) 36%, rgba(0, 0, 0, 0) 82%);
		pointer-events: none;
		z-index: 1;
	}

	.vn-stage {
		min-height: 100vh;
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		grid-template-rows: auto 1fr auto;
		gap: clamp(1rem, 2vw, 2rem);
		padding: clamp(1rem, 2vw, 1.8rem) clamp(1.25rem, 3vw, 2.6rem) clamp(1.8rem, 4vw, 2.9rem);
		position: relative;
		z-index: 2;
	}

	.status-shell {
		grid-column: 1 / -1;
		grid-row: 1;
	}

	.subtitle-shell {
		grid-column: 1;
		grid-row: 3;
		align-self: end;
		max-width: min(54rem, 70vw);
		padding-right: 2rem;
	}

	.subtitle-kicker {
		font-family: var(--mono);
		font-size: 0.74rem;
		letter-spacing: 0.22em;
		text-transform: uppercase;
		color: var(--text-secondary);
		text-shadow: var(--mono-shadow);
		margin-bottom: 1rem;
	}

	.subtitle-title {
		font-family: var(--serif-display);
		font-size: clamp(3.4rem, 9vw, 7rem);
		line-height: 0.84;
		font-weight: 600;
		letter-spacing: 0.03em;
		text-transform: none;
		text-shadow: var(--subtitle-shadow);
		margin-bottom: 0.9rem;
	}

	.subtitle-line {
		font-family: var(--serif-body);
		font-size: clamp(1.02rem, 1.7vw, 1.3rem);
		line-height: 1.9;
		letter-spacing: 0.03em;
		text-transform: none;
		color: var(--text-primary);
		text-shadow: var(--subtitle-shadow);
	}

	.subtitle-line-zh {
		color: var(--text-secondary);
	}

	.subtitle-note {
		margin-top: 1.15rem;
		font-family: var(--mono);
		font-size: 0.72rem;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--text-muted);
		text-shadow: var(--mono-shadow);
	}

	.edge-nav {
		grid-column: 2;
		grid-row: 3;
		align-self: end;
		justify-self: end;
		display: flex;
		flex-direction: column;
		align-items: flex-end;
		gap: 0.5rem;
		min-width: 13rem;
		padding-bottom: 0.1rem;
	}

	.edge-nav a {
		display: inline-flex;
		align-items: center;
		justify-content: flex-end;
		gap: 0.55rem;
		font-family: var(--mono);
		font-size: 0.82rem;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		text-decoration: none;
		color: var(--text-secondary);
		text-shadow: var(--mono-shadow);
		opacity: 0.7;
		transition: opacity 0.18s ease, color 0.18s ease;
	}

	.edge-nav a:hover,
	.edge-nav a:focus-visible {
		color: var(--text-primary);
		opacity: 1;
		outline: none;
	}

	.nav-arrow {
		opacity: 0;
		transition: opacity 0.18s ease;
	}

	.edge-nav a:hover .nav-arrow,
	.edge-nav a:focus-visible .nav-arrow {
		opacity: 1;
	}

	.nav-label {
		text-align: right;
	}

	@media (max-width: 900px) {
		body {
			overflow: auto;
		}

		.vn-stage {
			grid-template-columns: 1fr;
			grid-template-rows: auto 1fr auto auto;
			padding-bottom: 2rem;
		}

		.subtitle-shell,
		.edge-nav {
			grid-column: 1;
			justify-self: start;
			max-width: none;
			padding-right: 0;
		}

		.edge-nav {
			grid-row: 4;
			align-items: flex-start;
			margin-top: 0.8rem;
			min-width: 0;
		}

		.edge-nav a {
			justify-content: flex-start;
		}

		.nav-label {
			text-align: left;
		}
	}

	@media (max-width: 640px) {
		body {
			background:
				linear-gradient(rgba(8, 8, 8, 0.26), rgba(8, 8, 8, 0.18)),
				url("/bg-poster.jpg") center / cover no-repeat fixed;
		}

		.subtitle-fade {
			height: 50vh;
		}

		.subtitle-title {
			font-size: 2.8rem;
		}

		.subtitle-line {
			font-size: 0.98rem;
			line-height: 1.8;
		}
	}
</style>
